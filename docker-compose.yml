services:
  meilisearch:
    image: getmeili/meilisearch:v1.10
    ports:
      - "7700:7700"
    environment:
      MEILI_MASTER_KEY: lingsou-dev-key
      MEILI_ENV: development
    volumes:
      - ./data/meili:/meili_data
